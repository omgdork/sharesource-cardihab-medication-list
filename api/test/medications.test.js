describe('Medications', () => {
  describe('/search_medication', () => {
    it('should throw an error if medication is not provided.', () => {

    });

    it('should throw an error if authentication token is not provided.', () => {

    });

    it('should send a list of medications if there are any.', () => {

    });

    it('should send error details when an item is not found.', () => {

    });
  });
});
